import type { RequestStatus } from "@/lib/types/data-sharing/request.types";

export interface RequestStatusBadgeProps {
  status: RequestStatus;
}

export function RequestStatusBadge(_props: RequestStatusBadgeProps) {
  return null;
}
